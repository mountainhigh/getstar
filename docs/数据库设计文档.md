# 儿童行为激励管理微信小程序 - 数据库设计文档

**版本**: v1.4
**日期**: 2026-03-08
**作者**: AI Assistant
**状态**: 正式版

---

**更新说明 (v1.4)**:
- 更新 `children` 集合：明确 `avatar` 字段支持图片URL，新增 `avatarType` 字段。

---

## 1. 数据库选型

### 1.1 技术选型
- **数据库**: CloudBase 文档数据库（NoSQL）
- **特点**: 
  - 与微信小程序无缝集成
  - 支持 JSON 文档存储
  - 自动扩展
  - 免费额度充足

---

## 2. 数据库集合设计

### 2.1 users（用户表）

**集合说明**: 存储用户基本信息

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 用户OpenID（系统自动注入） |
| nickname | String | 否 | 用户昵称 |
| avatar | String | 否 | 头像URL |
| familyId | String | 否 | 关联的家庭ID |
| createTime | Date | 是 | 创建时间 |
| updateTime | Date | 是 | 更新时间 |

---

### 2.2 families（家庭表）

**集合说明**: 存储家庭基本信息

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| name | String | 是 | 家庭名称 |
| ownerId | String | 是 | 创建人ID (关联users表的_id) |
| createTime | Date | 是 | 创建时间 |
| updateTime | Date | 是 | 更新时间 |

---

### 2.3 children（孩子表）

**集合说明**: 存储孩子基本信息

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| familyId | String | 是 | 家庭ID |
| name | String | 是 | 孩子姓名 |
| avatar | String | 否 | 头像内容（Emoji字符 或 图片URL） |
| avatarType | String | 否 | 头像类型 ('emoji' 或 'image')，默认为 'emoji' |
| points | Number | 是 | 总星星 |
| coins | Number | 是 | 金币数量 |
| level | Number | 是 | 当前等级 |
| title | String | 否 | 当前称号 |
| createTime | Date | 是 | 创建时间 |
| updateTime | Date | 是 | 更新时间 |

---

### 2.4 habits（习惯表）

**集合说明**: 存储每个孩子的习惯设置

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| childId | String | 是 | 孩子ID |
| familyId | String | 是 | 家庭ID |
| name | String | 是 | 习惯名称 |
| icon | String | 是 | 图标emoji |
| category | String | 是 | 分类 (study/life/exercise/habit/social) |
| categoryName | String | 是 | 分类中文名称 |
| color | String | 是 | 分类颜色 |
| points | Number | 是 | 星星值 |
| order | Number | 是 | 排序 |
| isActive | Boolean | 是 | 是否激活 |
| templateId | String | 否 | 关联的模板ID |
| createTime | Date | 是 | 创建时间 |
| updateTime | Date | 是 | 更新时间 |

---

### 2.5 check_ins（打卡记录表）

**集合说明**: 存储打卡记录

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| childId | String | 是 | 孩子ID |
| habitId | String | 是 | 习惯ID |
| date | String | 是 | 日期 YYYY-MM-DD |
| points | Number | 是 | 获得星星 |
| habitPoints | Number | 是 | 习惯原本设定的星星值 |
| coins | Number | 是 | 获得金币 |
| photos | Array | 否 | 照片URL数组 |
| remark | String | 否 | 备注 |
| createTime | Date | 是 | 创建时间 |

---

### 2.6 habit_templates（习惯模板表）

**集合说明**: 全局共享的习惯模板数据

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| id | String | 是 | 模板ID |
| name | String | 是 | 习惯名称 |
| icon | String | 是 | 图标emoji |
| category | String | 是 | 分类 |
| categoryName | String | 是 | 分类中文名称 |
| color | String | 是 | 分类颜色 |
| points | Number | 是 | 推荐星星值 |
| order | Number | 是 | 排序 |

---

### 2.7 rewards（礼物表）

**集合说明**: 存储礼物信息（支持全局模板和用户自定义）

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 否 | 创建者OpenID（系统自动注入，全局模板无此字段） |
| name | String | 是 | 礼物名称 |
| icon | String | 是 | 图标emoji |
| description | String | 否 | 描述 |
| coinCost | Number | 是 | 金币成本 |
| stock | Number | 是 | 库存（-1表示无限） |
| category | String | 是 | 分类 |
| order | Number | 是 | 排序 |

---

### 2.8 reward_exchanges（礼物兑换记录表）

**集合说明**: 存储礼物兑换记录

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| childId | String | 是 | 孩子ID |
| rewardId | String | 是 | 礼物ID |
| rewardName | String | 是 | 礼物名称 |
| coinCost | Number | 是 | 消耗金币 |
| status | String | 是 | 状态 (pending/completed/cancelled) |
| createTime | Date | 是 | 创建时间 |

---

### 2.9 coin_records（金币变动记录表）

**集合说明**: 存储金币的增加和消耗记录

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| childId | String | 是 | 孩子ID |
| amount | Number | 是 | 变动数量（正数增加，负数减少） |
| type | String | 是 | 类型 (check_in/exchange/refund/manual) |
| description | String | 否 | 描述 |
| relatedId | String | 否 | 关联ID（如checkInId或exchangeId） |
| createTime | Date | 是 | 创建时间 |

---

### 2.10 badges（称号表）

**集合说明**: 全局共享的称号定义

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| id | String | 是 | 称号ID |
| name | String | 是 | 称号名称 |
| icon | String | 是 | 图标emoji |
| description | String | 是 | 描述 |
| condition | String | 是 | 获得条件说明 |
| pointsRequired | Number | 是 | 所需星星 |
| color | String | 是 | 颜色 |
| rarity | String | 是 | 稀有度 |
| category | String | 是 | 分类 |
| order | Number | 是 | 排序 |

---

### 2.11 user_badges（用户称号表）

**集合说明**: 存储孩子获得的称号

**字段说明**:

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| _id | String | 是 | 主键（自动生成） |
| _openid | String | 是 | 创建者OpenID（系统自动注入） |
| childId | String | 是 | 孩子ID |
| badgeId | String | 是 | 称号ID |
| earnedDate | Date | 是 | 获得日期 |
| isCurrent | Boolean | 是 | 是否当前佩戴 |
| createTime | Date | 是 | 创建时间 |
